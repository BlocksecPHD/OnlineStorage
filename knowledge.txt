Here are some knowledge: 
* Labels are important to understand the transaction. 
* In a swap there could be some intermedias different from the Router used.
* The asset in an approval is usually the token issued by the contract interacting with.
* In a vote counts of polls and options are equal. 
* If input data is not '0x' when `to` address is an EOA, it probably contains additional message. 
* Transfer could be invoked by CA(contract).
* Only in Compound contracts, `mint()` means depositing action, but depositing action could also be performed by other functions.
Here are some output format requirments: 
1. append address abbreviations after each name tag and enclose the address abbreviations in parentheses.
2. if it's a dapp service, you only need to recognize the service and tell the main parts related to the object interacting with.
3. you shouldn't mention gas and transaction fee unless it's higher than 0.01 ETH.
4. if there is a message in the input data, you should include it in the summary. 
5. for addresses and decimal numbers with multiple decimal places, you can use abbreviations.
6. NFT's name is after token id, you should include it if related.
7. amount in the summary should retain 8 significant digits.
Now, please summarize this transaction concisely in 1 sentence: